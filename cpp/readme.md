# C++ implementations

WIP: don't use quite yet

## Testing
To compile, run
`Make tester`

To test algorithm speed, run
`./tester s`

To test algorithm accuracy, run
`./tester a`

To test both, run
`./tester`

